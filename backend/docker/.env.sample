# Docker Compose Environment Variables Sample
# Copy this file to '.env' and update with your actual values
# 
# This file contains default passwords for development/testing
# CHANGE ALL PASSWORDS for production deployments!

# Docker Compose Project Name (prevents conflicts with other projects)
COMPOSE_PROJECT_NAME=toolssharing

# PostgreSQL Database Configuration
POSTGRES_PASSWORD=ToolsPassword123!

# Redis Cache Configuration  
# Note: Redis will require authentication with this password
REDIS_PASSWORD=RedisPassword123!

# MinIO Object Storage Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=MinIOPassword123!

# Important Notes:
# - These are DEFAULT passwords for development
# - Generate strong passwords for production
# - All services will use authentication with these credentials
# - Changing passwords requires updating config.json connection strings